{
  "//": "FIXTURE tsconfig — input to the cq-design-system-foundation benchmark (check P2). Compiles fixture.tsx, which passes VALID token values to the same token-typed props; `tsc --noEmit` MUST exit zero. @cq/ui + @cq/tokens resolve via the repo's node_modules workspace symlinks.",
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2023", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "moduleResolution": "bundler",
    "jsx": "react-jsx",
    "strict": true,
    "skipLibCheck": true,
    "noEmit": true,
    "typeRoots": ["../../../../node_modules/@types"]
  },
  "include": ["fixture.tsx"]
}
